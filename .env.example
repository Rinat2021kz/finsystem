# PostgreSQL (Neon / Vercel Postgres / локальный)
DATABASE_URL="postgresql://user:password@localhost:5432/finsystem"

# Auth.js — сгенерировать: npx auth secret  (или openssl rand -base64 32)
AUTH_SECRET="change-me"

# База URL приложения (для Auth.js за прокси)
AUTH_TRUST_HOST=true
